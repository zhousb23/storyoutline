# StoryOutline — Python 依赖
# 安装方式：pip install -r requirements.txt

# UI 框架（Qt for Python）
PySide6>=6.5,<7.0

# HTTP 请求（DeepSeek API）
requests>=2.28,<3.0

# Word 文档读取与写入
python-docx>=1.0,<2.0

# PDF 解析
pdfplumber>=0.9,<1.0

# 文本编码自动检测
chardet>=5.0,<6.0

# EPUB 电子书解析
ebooklib>=0.18
beautifulsoup4>=4.12

# 打包（开发阶段不需要，最终阶段使用）
# pyinstaller>=6.0
